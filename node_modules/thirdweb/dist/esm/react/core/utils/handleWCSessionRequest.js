import { isAndroid, isIOS } from "./isMobile.js";
import { openWindow } from "./openWindow.js";
/**
 *
 * @internal
 */
export function handelWCSessionRequest(uris) {
    if (isAndroid()) {
        openWindow(uris.android);
    }
    else if (isIOS()) {
        openWindow(uris.ios);
    }
    else {
        openWindow(uris.other);
    }
}
//# sourceMappingURL=handleWCSessionRequest.js.map