import { jsx as _jsx } from "react/jsx-runtime";
import { useChainQuery } from "../../../core/hooks/others/useChainQuery.js";
import { genericTokenIcon } from "../ConnectWallet/icons/dataUris.js";
import { isNativeToken, } from "../ConnectWallet/screens/nativeToken.js";
import { iconSize } from "../design-system/index.js";
import { Img } from "./Img.js";
/**
 * @internal
 */
export function TokenIcon(props) {
    const token = props.token;
    if (isNativeToken(token)) {
        return (_jsx(NativeTokenIcon, { chain: props.chain, nativeToken: token, size: props.size }));
    }
    return (_jsx(Img, { src: token.icon, width: iconSize[props.size], height: iconSize[props.size], fallbackImage: genericTokenIcon }));
}
function NativeTokenIcon(props) {
    const chainQuery = useChainQuery(props.chain);
    return (_jsx(Img, { src: chainQuery.data?.icon?.url, width: iconSize[props.size], height: iconSize[props.size], fallbackImage: genericTokenIcon }));
}
//# sourceMappingURL=TokenIcon.js.map