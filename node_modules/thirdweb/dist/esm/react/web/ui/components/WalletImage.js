import { jsx as _jsx } from "react/jsx-runtime";
import { getMIPDStore } from "../../../../wallets/injected/mipdStore.js";
import { radius } from "../design-system/index.js";
import { useWalletImage } from "../hooks/useWalletInfo.js";
import { Img } from "./Img.js";
/**
 * @internal
 */
export function WalletImage(props) {
    const mipdImage = getMIPDStore()
        .getProviders()
        .find((provider) => provider.info.rdns === props.id)?.info.icon;
    if (mipdImage) {
        return (_jsx(Img, { src: mipdImage, width: props.size, height: props.size, loading: "eager", style: {
                borderRadius: radius.md,
            } }));
    }
    return _jsx(WalletImageQuery, { id: props.id, size: props.size });
}
function WalletImageQuery(props) {
    const walletImage = useWalletImage(props.id);
    return (_jsx(Img, { src: walletImage.data, width: props.size, height: props.size, loading: "eager", style: {
            borderRadius: radius.md,
        } }));
}
//# sourceMappingURL=WalletImage.js.map