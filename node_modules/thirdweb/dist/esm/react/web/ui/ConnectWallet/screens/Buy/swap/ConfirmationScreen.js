import { jsx as _jsx, jsxs as _jsxs, Fragment as _Fragment } from "react/jsx-runtime";
import { CheckCircledIcon, CheckIcon, CrossCircledIcon, } from "@radix-ui/react-icons";
import { useState } from "react";
import { useSendTransaction } from "../../../../../../core/hooks/contract/useSend.js";
import { useChainQuery } from "../../../../../../core/hooks/others/useChainQuery.js";
import { useBuyWithCryptoStatus, } from "../../../../../../core/hooks/pay/useBuyWithCryptoStatus.js";
// import { useActiveWallet } from "../../../../../../core/hooks/wallets/wallet-hooks.js";
import { shortenString } from "../../../../../../core/utils/addresses.js";
import { formatNumber } from "../../../../../../../utils/formatNumber.js";
import { Skeleton } from "../../../../components/Skeleton.js";
import { Spacer } from "../../../../components/Spacer.js";
import { Spinner } from "../../../../components/Spinner.js";
import { Container, ModalHeader } from "../../../../components/basic.js";
import { Button } from "../../../../components/buttons.js";
import { Text } from "../../../../components/text.js";
import { useCustomTheme } from "../../../../design-system/CustomThemeProvider.js";
import { StyledDiv } from "../../../../design-system/elements.js";
import { fontSize, iconSize, radius, spacing, } from "../../../../design-system/index.js";
import { isNativeToken } from "../../nativeToken.js";
import { SwapFees } from "./SwapFees.js";
import { addPendingSwapTransaction } from "./pendingSwapTx.js";
import { TokenIcon } from "../../../../components/TokenIcon.js";
import { waitForReceipt } from "../../../../../../../transaction/actions/wait-for-tx-receipt.js";
import { AccentFailIcon } from "../../../icons/AccentFailIcon.js";
import { WalletImage } from "../../../../components/WalletImage.js";
import { useActiveWallet } from "../../../../../../core/hooks/wallets/wallet-hooks.js";
/**
 * @internal
 */
export function ConfirmationScreen(props) {
    const activeWallet = useActiveWallet();
    const sendTransactionMutation = useSendTransaction();
    const [swapTx, setSwapTx] = useState();
    const isApprovalRequired = props.buyWithCryptoQuote.approval !== undefined;
    const [step, setStep] = useState(isApprovalRequired ? "approval" : "swap");
    const [status, setStatus] = useState("idle");
    const fromChain = useChainQuery(props.fromChain);
    const toChain = useChainQuery(props.toChain);
    const fromTokenSymbol = isNativeToken(props.fromToken)
        ? fromChain.data?.nativeCurrency?.symbol
        : props.fromToken?.symbol;
    const toTokenSymbol = isNativeToken(props.toToken)
        ? toChain.data?.nativeCurrency?.symbol
        : props.toToken?.symbol;
    if (swapTx) {
        return (_jsx(WaitingForConfirmation, { onBack: () => {
                props.onBack();
            }, onViewPendingTx: props.onViewPendingTx, swapTx: swapTx }));
    }
    return (_jsxs(Container, { p: "lg", children: [_jsx(ModalHeader, { title: "Confirm Buy", onBack: props.onBack }), _jsx(Spacer, { y: "lg" }), _jsx(TokenSelection, { label: "You Receive", chain: props.toChain, amount: String(formatNumber(Number(props.toAmount), 4)), symbol: toTokenSymbol || "", token: props.toToken }), _jsx(Spacer, { y: "lg" }), _jsx(TokenSelection, { label: "You Pay", chain: props.fromChain, amount: String(formatNumber(Number(props.fromAmount), 4)), symbol: fromTokenSymbol || "", token: props.fromToken }), _jsx(Spacer, { y: "lg" }), _jsx(Text, { size: "sm", color: "secondaryText", children: "Recipient Address" }), _jsx(Spacer, { y: "xs" }), _jsx(TokenInfoContainer, { children: _jsxs(Container, { flex: "row", gap: "md", center: "y", children: [activeWallet?.id && (_jsx(WalletImage, { size: iconSize.lg, id: activeWallet.id })), _jsx(Text, { color: "primaryText", size: "sm", children: shortenString(props.account.address, false) })] }) }), _jsx(Spacer, { y: "lg" }), _jsx(SwapFees, { quote: props.buyWithCryptoQuote }), _jsx(Spacer, { y: "md" }), isApprovalRequired && (_jsxs(_Fragment, { children: [_jsx(Spacer, { y: "sm" }), _jsxs(Container, { gap: "sm", flex: "row", style: {
                            justifyContent: "space-between",
                        }, center: "y", color: "accentText", children: [_jsx(Step, { isDone: step === "swap", isActive: step === "approval", label: step === "approval" ? "Approve" : "Approved" }), _jsx(ConnectorLine, {}), _jsx(Step, { isDone: false, label: "Confirm", isActive: step === "swap" })] }), _jsx(Spacer, { y: "lg" })] })), status === "error" && (_jsxs(_Fragment, { children: [_jsxs(Container, { flex: "row", gap: "xs", center: "y", color: "danger", children: [_jsx(CrossCircledIcon, { width: iconSize.sm, height: iconSize.sm }), _jsx(Text, { color: "danger", size: "sm", children: step === "approval" ? "Failed to Approve" : "Failed to Confirm" })] }), _jsx(Spacer, { y: "md" })] })), _jsxs(Button, { variant: "accent", fullWidth: true, disabled: status === "pending", onClick: async () => {
                    if (step === "approval" && props.buyWithCryptoQuote.approval) {
                        try {
                            setStatus("pending");
                            const tx = await sendTransactionMutation.mutateAsync(props.buyWithCryptoQuote.approval);
                            await waitForReceipt({ ...tx, maxBlocksWaitTime: 50 });
                            props.onQuoteFinalized(props.buyWithCryptoQuote);
                            setStep("swap");
                            setStatus("idle");
                        }
                        catch (e) {
                            console.error(e);
                            setStatus("error");
                        }
                    }
                    if (step === "swap") {
                        setStatus("pending");
                        try {
                            const _swapTx = await sendTransactionMutation.mutateAsync(props.buyWithCryptoQuote.transactionRequest);
                            await waitForReceipt({ ..._swapTx, maxBlocksWaitTime: 50 });
                            props.onQuoteFinalized(props.buyWithCryptoQuote);
                            // these will be defined by this time
                            if (fromTokenSymbol && toTokenSymbol && fromChain.data) {
                                addPendingSwapTransaction(props.client, {
                                    from: {
                                        symbol: fromTokenSymbol,
                                        value: props.fromAmount,
                                        chainId: props.fromChain.id,
                                    },
                                    to: {
                                        symbol: toTokenSymbol,
                                        value: props.toAmount,
                                    },
                                    status: "PENDING",
                                    transactionHash: _swapTx.transactionHash, // ?? _swapTx.userOpHash,
                                });
                            }
                            setSwapTx({
                                transactionHash: _swapTx.transactionHash, // ?? _swapTx.userOpHash,
                                client: props.client,
                            });
                        }
                        catch (e) {
                            console.error(e);
                            setStatus("error");
                        }
                    }
                }, gap: "xs", children: [step === "approval" ? "Approve" : "Confirm", status === "pending" && _jsx(Spinner, { size: "sm", color: "accentButtonText" })] })] }));
}
const ConnectorLine = /* @__PURE__ */ StyledDiv(() => {
    const theme = useCustomTheme();
    return {
        height: "1.5px",
        background: theme.colors.secondaryText,
        flex: 1,
    };
});
function Step(props) {
    return (_jsxs(Container, { flex: "row", center: "y", gap: "xs", style: {
            fontSize: fontSize.sm,
        }, color: props.isDone
            ? "success"
            : props.isActive
                ? "primaryText"
                : "secondaryText", children: [_jsx(Circle, { children: props.isDone ? (_jsx(CheckIcon, { width: iconSize.sm, height: iconSize.sm })) : (_jsx("div", { style: {
                        background: "currentColor",
                        width: "7px",
                        height: "7px",
                        borderRadius: "50%",
                    } })) }), props.label] }));
}
const Circle = /* @__PURE__ */ StyledDiv(() => {
    return {
        border: `1px solid currentColor`,
        width: "20px",
        height: "20px",
        borderRadius: "50%",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
    };
});
function TokenSelection(props) {
    const chainQuery = useChainQuery(props.chain);
    return (_jsxs("div", { children: [_jsx(Text, { size: "sm", color: "secondaryText", children: props.label }), _jsx(Spacer, { y: "xxs" }), _jsx(TokenInfoContainer, { children: _jsxs(Container, { flex: "row", gap: "md", center: "y", children: [_jsx(TokenIcon, { token: props.token, chain: props.chain, size: "lg" }), _jsxs(Container, { flex: "column", gap: "xxs", children: [_jsxs(Text, { color: "primaryText", size: "sm", children: [props.amount, " ", props.symbol] }), chainQuery.data ? (_jsx(Text, { size: "xs", children: chainQuery.data.name })) : (_jsx(Skeleton, { width: "100px", height: fontSize.xs }))] })] }) })] }));
}
function WaitingForConfirmation(props) {
    const swapStatus = useBuyWithCryptoStatus(props.swapTx);
    const isSuccess = swapStatus.data?.status === "COMPLETED";
    const isFailed = swapStatus.data?.status === "FAILED";
    const isPending = !isSuccess && !isFailed;
    return (_jsx(Container, { animate: "fadein", children: _jsxs(Container, { p: "lg", children: [_jsx(ModalHeader, { title: "Buy", onBack: props.onBack }), _jsx(Spacer, { y: "xxl" }), _jsx(Spacer, { y: "xxl" }), _jsxs(Container, { flex: "column", animate: "fadein", center: "both", color: isSuccess ? "success" : isFailed ? "danger" : "accentText", children: [isSuccess ? (_jsx(CheckCircledIcon, { width: iconSize["3xl"], height: iconSize["3xl"] })) : isFailed ? (_jsx(AccentFailIcon, { size: iconSize["3xl"] })) : (_jsx(Spinner, { size: "3xl", color: "accentText" })), _jsx(Spacer, { y: "xxl" }), _jsxs(Text, { color: "primaryText", size: "lg", children: [" ", isSuccess
                                    ? "Buy Success"
                                    : isFailed
                                        ? "Transaction Failed"
                                        : "Buy Pending", " "] }), isPending && (_jsxs(_Fragment, { children: [_jsx(Spacer, { y: "md" }), _jsx(Text, { size: "sm", children: "Your transaction is currently pending" })] })), isFailed && (_jsxs(_Fragment, { children: [_jsx(Spacer, { y: "md" }), _jsxs(Text, { size: "sm", children: ["Your transaction ", `couldn't`, " be processed"] })] }))] }), _jsx(Spacer, { y: "xxl" }), !isFailed && (_jsx(Button, { variant: "accent", fullWidth: true, onClick: props.onViewPendingTx, children: "View Transactions" })), isFailed && (_jsx(Button, { variant: "accent", fullWidth: true, onClick: props.onBack, children: "Try Again" }))] }) }));
}
const TokenInfoContainer = /* @__PURE__ */ StyledDiv(() => {
    const theme = useCustomTheme();
    return {
        padding: spacing.sm,
        borderRadius: radius.md,
        background: theme.colors.walletSelectorButtonHoverBg,
        display: "flex",
        alignItems: "center",
    };
});
//# sourceMappingURL=ConfirmationScreen.js.map