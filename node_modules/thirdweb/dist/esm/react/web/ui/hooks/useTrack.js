import { useCallback } from "react";
import { track } from "../../../../analytics/track.js";
/**
 * @internal
 */
export function useTrack(client) {
    return useCallback((data) => {
        // never be blocked by tracking - error or otherwise
        setTimeout(() => {
            track(client, data);
        });
    }, [client]);
}
//# sourceMappingURL=useTrack.js.map