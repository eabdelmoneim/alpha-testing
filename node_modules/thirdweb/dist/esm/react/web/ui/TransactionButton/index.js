import { jsx as _jsx } from "react/jsx-runtime";
import { useState } from "react";
import { waitForReceipt as doWaitForReceipt, } from "../../../../transaction/actions/wait-for-tx-receipt.js";
import { useSendTransactionCore } from "../../../core/hooks/contract/useSendTransaction.js";
import { useActiveAccount, useActiveWallet, } from "../../../core/hooks/wallets/wallet-hooks.js";
import { Spinner } from "../components/Spinner.js";
import { Button } from "../components/buttons.js";
/**
 * TransactionButton component is used to render a button that triggers a transaction.
 * - It shows a "Switch Network" button if the connected wallet is on a different chain than the transaction.
 * @param props - The props for this component.
 * Refer to [TransactionButtonProps](https://portal.thirdweb.com/references/typescript/v5/TransactionButtonProps) for details.
 * @example
 * ```tsx
 * <TransactionButton
 *   transaction={() => {}}
 *   onSuccess={handleSuccess}
 *   onError={handleError}
 * >
 *   Confirm Transaction
 * </TransactionButton>
 * ```
 * @component
 */
export function TransactionButton(props) {
    const { children, transaction, onTransactionSent, onTransactionConfirmed, onError, onClick, ...buttonProps } = props;
    const account = useActiveAccount();
    const wallet = useActiveWallet();
    const [isPending, setIsPending] = useState(false);
    const sendTransaction = useSendTransactionCore();
    if (!isPending) {
        return (_jsx(Button, { gap: "xs", ...buttonProps, disabled: !account, variant: "primary", "data-is-loading": isPending, onClick: async (e) => {
                if (onClick) {
                    onClick(e);
                }
                try {
                    setIsPending(true);
                    const resolvedTx = await transaction();
                    if (wallet && wallet.getChain()?.id !== resolvedTx.chain.id) {
                        await wallet?.switchChain(resolvedTx.chain);
                    }
                    const result = await sendTransaction.mutateAsync(resolvedTx);
                    if (onTransactionSent) {
                        onTransactionSent(result);
                    }
                    if (onTransactionConfirmed) {
                        const receipt = await doWaitForReceipt(result);
                        onTransactionConfirmed(receipt);
                    }
                }
                catch (error) {
                    if (onError) {
                        onError(error);
                    }
                }
                finally {
                    setIsPending(false);
                }
            }, style: {
                ...buttonProps.style,
                opacity: !account ? 0.5 : 1,
            }, children: children }));
    }
    return (_jsx(Button, { ...buttonProps, disabled: true, variant: "primary", style: {
            ...buttonProps.style,
            minWidth: "150px",
        }, children: _jsx(Spinner, { size: "md", color: "primaryButtonText" }) }));
}
//# sourceMappingURL=index.js.map