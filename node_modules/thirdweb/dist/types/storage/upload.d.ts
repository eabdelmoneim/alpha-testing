import type { ThirdwebClient } from "../client/client.js";
import type { UploadOptions as InternalUploadOptions } from "./upload/types.js";
export type UploadOptions = InternalUploadOptions & {
    client: ThirdwebClient;
};
/**
 * Uploads files based on the provided options.
 * @param options - The upload options.
 * @returns A promise that resolves to an array of uploaded file URIs.
 * @throws An error if the upload fails.
 * @example
 * ```ts
 * import { upload } from "thirdweb/storage";
 * const uris = await upload({
 *  client,
 *  files: [
 *    new File(["hello world"], "hello.txt"),
 *  ],
 * });
 * ```
 * @storage
 */
export declare function upload(options: UploadOptions): Promise<string[]>;
//# sourceMappingURL=upload.d.ts.map